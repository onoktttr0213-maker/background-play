<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DualWebPlayer" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- API 35 (Android 15) enforces edge-to-edge by default, which draws
             our content behind the status/nav bars and breaks the 50/50
             split-screen measurement. Opt back out so layout_weight="1"
             sizing stays predictable. -->
        <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    </style>
</resources>
